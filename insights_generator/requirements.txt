# =============================================================================
# INSIGHTS GENERATOR - DEPENDENCIES
# =============================================================================
# Install with: pip install -r insights_generator/requirements.txt

# RSS feed parsing
feedparser>=6.0.0

# Machine learning
xgboost>=2.0.0
lightgbm>=4.0.0
scikit-learn>=1.3.0

# Data manipulation (likely already installed)
pandas>=2.0.0
numpy>=1.24.0

# HTTP requests (likely already installed)
requests>=2.31.0

# Ollama Python client (optional - can use HTTP directly)
# ollama>=0.1.0
